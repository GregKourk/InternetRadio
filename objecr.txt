        Object[][] songlist = new String [15][2];           //Δημιουργία 2-διάστατου πίνακα που κρατάμε την απόσταση και το τράγουδι που αφοράSS την απόσταση






int i=0;                                                        //Μετρητής για την πρώτη στήλη του 2-διάστατου πίνακα που θα έχει την απόσταση 2 τραγουδιών
        for (int k=0; k<songs.length; k++) {                                       //Υπολογισμός των αποστάσεων όλων των τραγουδιών με το επιλεγμένο τραγούδι
            int j=0;                                                    //Μετρητής για την δεύτερη στήλη του 2-διάσταστου πίνακα που θα έχει τον τίτλο του τραγουδιού που έχει την υπολογισμένη απόσταση
            double distance_of_songs = distance(given_song, songs[k]);
            songlist[i][j] = distance_of_songs;                         //Πέρασμα απόστασης στην 1η στήλη του πίνακα
            songlist[i][j+1] = songs[k].getTitle();                     //Πέρασμα τίτλου τραγουδιού στην 2η στήλη του πίνακα
            i++;
        }